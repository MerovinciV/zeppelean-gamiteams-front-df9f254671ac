import { CustomValidators.ts } from './custom-validators';

describe('CustomValidators.Ts', () => {
  it('should create an instance', () => {
    expect(new CustomValidators.Ts()).toBeTruthy();
  });
});
